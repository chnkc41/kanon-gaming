import React from 'react'

const NotConfirmedPassword = () => {
    return (
        <span className="text-sm !text-red-500 block my-1">The password must be the same.</span>
    )
}

export default NotConfirmedPassword